# @lam/ssh — 1.0.0
#
# Generated by ``lamc init`` and intentionally retained as the stable flat
# export point. The implementation lives in uniquely named root modules.

from .ssh_client import SshConfig, Context, SshContext, SshKey, SshKeyPair, SshClient, SshSession, SshCommandResult, SshTcpConn

func tag() -> str {
    return "@lam/ssh@1.0.0"
}
